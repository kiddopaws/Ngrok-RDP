name: RDP Pinggy (Anti-Blokir)

on: workflow_dispatch

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 9999

    steps:
    - name: Aktifkan RDP
      run: |
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1

    - name: Buat User & Password
      run: |
        $password = "KopiHitam@99"
        $user = "Skyro"
        net user $user $password /add /y
        net localgroup administrators $user /add
        echo "USER_PASS=$password" >> $env:GITHUB_ENV

    - name: 🚀 JALANKAN TUNNEL (Tunggu Link Muncul)
      run: |
        Write-Host "Sedang meminta link public dari Pinggy..."
        
        # Menggunakan SSH Tunneling ke Pinggy (Mode TCP)
        # Output akan langsung muncul di log
        ssh -o StrictHostKeyChecking=no -p 443 -R0:localhost:3389 tcp@a.pinggy.io 

    - name: Loop
      run: |
        while ($true) { Start-Sleep -Seconds 300 }
